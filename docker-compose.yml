version: "3.9"

services:

  backend:
    build: ./plateforme-back
    container_name: backend
    ports:
      - "8000:8000"
    env_file:
      - ./plateforme-back/.env
    depends_on:
      - db

  frontend:
    build: ./plateforme-tests
    container_name: frontend
    ports:
      - "3000:3000"
    depends_on:
      - backend

  db:
    image: postgres:15
    container_name: postgres_db
    restart: always
    environment:
      POSTGRES_USER: mohamedtaha
      POSTGRES_PASSWORD: mou7@peha
      POSTGRES_DB: plateforme
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  nginx:
    image: nginx:latest
    container_name: nginx
    ports:
      - "80:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf
    depends_on:
      - frontend
      - backend

volumes:
  postgres_data: